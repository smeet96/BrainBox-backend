datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
generator client {
  provider = "prisma-client-js"
}

model User {
id  Int @unique @default(autoincrement())
username String 
email String @unique 
password String 
content Content[]
}

model Content {
  id Int @unique @default(autoincrement())
  title String
  description String
  date DateTime? @default(now())
  author User @relation(fields: [authorId] , references: [id])
  authorId Int
  category Category[]
}

model Category {
  id Int @unique @default(autoincrement())
  type type 
  category Content @relation(fields: [contentId], references: [id])
  contentId Int
}

enum type {
  tweet
  video
  document
  Link
}